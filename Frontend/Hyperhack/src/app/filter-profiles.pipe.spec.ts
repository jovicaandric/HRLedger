import { FilterProfilesPipe } from './filter-profiles.pipe';

describe('FilterProfilesPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProfilesPipe();
    expect(pipe).toBeTruthy();
  });
});
